<p id="notice"><%= notice %></p>
<h1><%= @list.title %></h1>
<p><%= @list.body %></p>
<p>Shared to: <%= @list.affiliated_users.pluck(:email).join(', ') %>


<%= render 'items', items: @items, list: @list %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
